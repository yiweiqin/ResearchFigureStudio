# Legacy reference-specific rebuilds

These scripts document the repository's early image-to-PPT experiments. They intentionally remain outside the reusable `rfs` package because they contain sample-specific assumptions and historical local paths.

Use them only as implementation references for visual details. Do not import them from production workflows, plugin skills, tests, or packaging code. New reusable behavior belongs in `rfs/` with tests and path-independent inputs.
